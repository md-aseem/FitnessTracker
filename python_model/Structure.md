

## Input Handling
The proposed architecture is that the input would come from input.Yamel and go directly into input config in properties.buy properties.buy, and then it will populate all the other config based on the input config the job of converting in config into system config through intermediate config will be handled by process input by so what will happen is let's say you pass a cell CATL306 it will take that cell. The process in put file and it will extract heat generation. It will extract OCV. It will extract other properties and build battery config, and then the combination of battery chiller environment and control config is system config, which will then feed into the simulation itself and simulation will be run.
